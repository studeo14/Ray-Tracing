[package]
name = "ray-tracing"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
image = "^0.24"
rand = "^0.8"
threadpool = "^1.8"

[profile.release]
debug = 1
